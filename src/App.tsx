import React from 'react';
import {
  SafeAreaView,
  ScrollView,
  StatusBar,
  StyleSheet,
  Text,
  useColorScheme,
  View,
} from 'react-native';

function App(): JSX.Element {
  return (
    <ScrollView>
      <Text>Currency App</Text>
    </ScrollView>
  );
}

const styles = StyleSheet.create({});

export default App;
